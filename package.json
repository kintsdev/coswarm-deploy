{
  "name": "coswarm-deploy-action",
  "version": "1.0.7",
  "description": "GitHub Action that triggers Coswarm deployments and files issues on failure.",
  "main": "build/index.mjs",
  "scripts": {
    "bundle": "ncc build src/index.mjs -o build",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.1"
  }
}
