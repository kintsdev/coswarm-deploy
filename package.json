{
  "name": "coswarm-deploy-action",
  "version": "1.0.7",
  "description": "GitHub Action that triggers Coswarm deployments and files issues on failure.",
  "main": "build/index.js",
  "scripts": {
    "bundle": "ncc build src/index.js -o build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^2.0.1",
    "@actions/github": "^7.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
